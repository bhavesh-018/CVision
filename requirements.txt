# ====================================================================
# CVision - AI Career Tool
# Backend Requirements
# Python 3.11+
# Install: pip install -r requirements.txt
# ====================================================================

# --- Core API Framework ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.27       # Required for FastAPI file/form uploads
watchfiles==1.1.1              # Required for uvicorn --reload

# --- AI & LLM (Google Gemini) ---
google-generativeai==0.8.6     # Gemini 2.5 Flash: resume rewrite, AI review, Q&A, RAG generation

# --- Vector Database & RAG Pipeline ---
chromadb==1.5.8                # ChromaDB: vector store for resume/GitHub/LinkedIn/job embeddings

# --- Semantic Embeddings ---
sentence-transformers==5.4.1   # all-MiniLM-L6-v2: semantic similarity, JD matching, ChromaDB embeddings
scikit-learn==1.7.2            # cosine_similarity for semantic match scoring

# --- Resume PDF Parsing ---
pypdf==6.10.2                  # Extract text from uploaded PDF resumes

# --- HTTP Client (Adzuna Job Search API, GitHub API, LinkedIn scraping) ---
httpx==0.28.1                  # Async HTTP client for external API calls

# --- Web Scraping (LinkedIn profile analysis) ---
beautifulsoup4==4.13.4         # HTML parsing for LinkedIn profile data

# --- Environment & Config ---
python-dotenv==1.1.1           # Load GEMINI_API_KEY, ADZUNA_APP_ID, ADZUNA_API_KEY from .env

# --- Data Validation ---
pydantic==2.11.7               # Request/response models for FastAPI endpoints
